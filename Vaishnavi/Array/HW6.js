// 6. Create an array with the names of three countries. Add two more countries to the array using the push() method.

const countries = ['USA', 'Canada', 'UK'];
console.log(countries);

countries.push('France', 'Germany');
console.log(countries);
