// 7. Create an empty array called history. Add three historical events to the history array using the push() method. Print the array after each addition.

const history = [];
history.push('Ancient Egypt');
console.log(history);

history.push('The Renaissance');
console.log(history);

history.push('World War II');
console.log(history);
